name: CardGame CI Pipeline

on:
  push:
    branches: [ "main", "phase-3" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run Dummy Test
      run: echo "Kodlar basariyla derlendi ve CI/CD testinden gecti!"